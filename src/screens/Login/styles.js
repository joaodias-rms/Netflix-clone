import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container:{
        backgroundColor: '#000',
        flex: 1,
        justifyContent: 'center',
        padding: 20,
    },
    logo:{
        display: 'flex',
        alignItems: 'center',
        height: 100,
        width: '100%',
        marginBottom:60
    },
    loginInput:{
        marginBottom: 20
    }
})
